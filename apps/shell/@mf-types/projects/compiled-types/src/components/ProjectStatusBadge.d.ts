import type { ProjectStatus } from "@mlops/contracts";
export declare function ProjectStatusBadge({ status, detail }: {
    status: ProjectStatus;
    detail?: boolean;
}): import("react").JSX.Element;
