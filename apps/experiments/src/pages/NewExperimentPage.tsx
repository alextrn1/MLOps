import type { CreateExperimentDto } from "@mlops/contracts";
import { AppIcon, Button, Notice, SelectField, TextField } from "@mlops/ui";
import { useState, type ChangeEvent, type FormEvent } from "react";
import { Link, useNavigate } from "react-router-dom";
import { experimentsApi } from "../api";
import { datasetOptions, modelOptions, projectOptions } from "../experimentViewModel";

type FormErrors = Partial<Record<keyof CreateExperimentDto, string>>;
const initial: CreateExperimentDto = { name: "", projectId: "", modelId: "", datasetId: "" };
export function NewExperimentPage() {
  const navigate = useNavigate(); const [form, setForm] = useState(initial); const [errors, setErrors] = useState<FormErrors>({}); const [submitting, setSubmitting] = useState(false); const [submitError, setSubmitError] = useState(""); const [success, setSuccess] = useState(false);
  const update = (field: keyof CreateExperimentDto, value: string) => { setForm((current) => ({ ...current, [field]: value })); setErrors((current) => ({ ...current, [field]: undefined })); };
  const submit = async (event: FormEvent) => { event.preventDefault(); const next: FormErrors = {}; if (!form.name.trim()) next.name = "Введите название прогона"; if (!form.projectId) next.projectId = "Выберите проект"; if (!form.modelId) next.modelId = "Выберите модель"; if (!form.datasetId) next.datasetId = "Выберите датасет"; setErrors(next); if (Object.keys(next).length) return; setSubmitting(true); setSubmitError(""); try { const created = await experimentsApi.createExperiment(form); setSuccess(true); navigate(`/experiments/${created.id}`); } catch { setSubmitError("Не удалось запустить эксперимент. Попробуйте снова."); setSubmitting(false); } };
  return <section className="experiments-page"><header className="new-experiment-heading"><Link className="experiment-back" to="/experiments" aria-label="Назад к экспериментам"><AppIcon name="arrowLeft" size={22} aria-hidden /></Link><div><h1>Запуск эксперимента</h1><p>Создайте новый прогон обучения модели</p></div></header><form className="new-experiment-card" onSubmit={submit} noValidate><div className="new-experiment-card__header"><h2>Параметры прогона</h2><p>Укажите связанные ресурсы и понятное имя эксперимента.</p></div><div className="new-experiment-fields">{submitError ? <Notice tone="error">{submitError}</Notice> : null}{success ? <Notice>Эксперимент запущен. Открываем карточку…</Notice> : null}<TextField label="Название прогона" placeholder="Например, xgb_features_tuning" value={form.name} onChange={(event: ChangeEvent<HTMLInputElement>) => update("name", event.target.value)} error={errors.name} disabled={submitting || success} /><SelectField label="Проект" value={form.projectId} onChange={(event: ChangeEvent<HTMLSelectElement>) => update("projectId", event.target.value)} error={errors.projectId} disabled={submitting || success}><option value="">Выберите проект</option>{projectOptions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</SelectField><div className="new-experiment-row"><SelectField label="Модель" value={form.modelId} onChange={(event: ChangeEvent<HTMLSelectElement>) => update("modelId", event.target.value)} error={errors.modelId} disabled={submitting || success}><option value="">Выберите модель</option>{modelOptions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</SelectField><SelectField label="Датасет" value={form.datasetId} onChange={(event: ChangeEvent<HTMLSelectElement>) => update("datasetId", event.target.value)} error={errors.datasetId} disabled={submitting || success}><option value="">Выберите датасет</option>{datasetOptions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</SelectField></div></div><div className="new-experiment-actions"><Button variant="secondary" type="button" disabled={submitting || success} onClick={() => navigate("/experiments")}>Отмена</Button><Button type="submit" disabled={submitting || success}>{submitting ? <span className="button-spinner" aria-hidden /> : <AppIcon name="play" size={16} aria-hidden />}{submitting ? "Запускаем…" : "Запустить прогон"}</Button></div></form></section>;
}
