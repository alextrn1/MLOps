import type { CreateDatasetVersionDto } from "@mlops/contracts";
import { AppIcon, Button, Card, DelayedLoadingState, EmptyState, ErrorState, Notice, TextField } from "@mlops/ui";
import { type FormEvent, useState } from "react";
import { Link, useLocation, useNavigate, useParams } from "react-router-dom";
import { datasetsApi, isDatasetNotFound } from "../api";
import { formatDate, formatNumber } from "../format";
import { useApiResource } from "../useApiResource";
import { DatasetNotFoundPage } from "./DatasetNotFoundPage";

export function DatasetDetailsPage() {
  const { datasetId = "" } = useParams(); const navigate = useNavigate(); const location = useLocation(); const [showVersion, setShowVersion] = useState(false); const [version, setVersion] = useState<CreateDatasetVersionDto>({ version: "", description: "" }); const [formError, setFormError] = useState(""); const [submitting, setSubmitting] = useState(false);
  const resource = useApiResource(() => Promise.all([datasetsApi.getDataset(datasetId), datasetsApi.listVersions(datasetId), datasetsApi.getLineage(datasetId)]), [datasetId]);
  async function submitVersion(event: FormEvent) { event.preventDefault(); if (!version.version.trim()) { setFormError("Укажите номер версии"); return; } setSubmitting(true); try { const created = await datasetsApi.createVersion(datasetId, version); navigate(`/datasets/${datasetId}/versions/${created.id}`); } finally { setSubmitting(false); } }
  if (resource.loading) return <section className="datasets-page"><DelayedLoadingState loading label="Загрузка карточки датасета…" /></section>;
  if (resource.error && isDatasetNotFound(resource.error)) return <DatasetNotFoundPage />;
  if (resource.error || !resource.data) return <section className="datasets-page"><ErrorState title="Не удалось загрузить датасет" description="Повторите попытку." onRetry={resource.retry} /></section>;
  const [dataset, versions, lineage] = resource.data;
  return <section className="datasets-page"><button className="back-link" onClick={() => navigate("/datasets")} aria-label="Назад"><AppIcon name="arrowLeft" size={22} aria-hidden /></button><header className="datasets-heading detail-heading"><div><h1>{dataset.name} <span className="version-chip large">{dataset.latestVersion}</span></h1><p>{dataset.description}</p></div><Button onClick={() => setShowVersion((value) => !value)}><AppIcon name="plus" size={18} aria-hidden />Зарегистрировать версию</Button></header>{(location.state as { success?: string } | null)?.success ? <Notice>{(location.state as { success: string }).success}</Notice> : null}<div className="dataset-summary"><Card><AppIcon name="database" size={25} aria-hidden /><span>Источник<strong>{dataset.sourceLabel}</strong></span></Card><Card><AppIcon name="folder" size={25} aria-hidden /><span>Проект<Link to={`/projects/${dataset.project.id}`}>{dataset.project.name}</Link></span></Card><Card><AppIcon name="file" size={25} aria-hidden /><span>Объем<strong>{formatNumber(dataset.sizeMb)} МБ</strong></span></Card><Card><AppIcon name="columns" size={25} aria-hidden /><span>Строк<strong>{dataset.rowsLabel}</strong></span></Card></div>{showVersion ? <form className="inline-version-form" onSubmit={submitVersion}><TextField label="Номер версии" value={version.version} onChange={(e) => setVersion((v) => ({ ...v, version: e.target.value }))} error={formError} placeholder="2.2.0" /><TextField label="Описание" value={version.description} onChange={(e) => setVersion((v) => ({ ...v, description: e.target.value }))} placeholder="Что изменилось" /><Button disabled={submitting}>{submitting ? "Сохранение…" : "Создать версию"}</Button></form> : null}<div className="detail-grid"><Card className="versions-card"><h2><AppIcon name="gitBranch" size={22} aria-hidden />История версий</h2>{versions.length ? <div className="versions-table"><div className="versions-head"><span>Версия</span><span>Объем</span><span>Строк</span><span>Автор / Дата</span><span /></div>{versions.map((item) => <div className="version-row" key={item.id}><Link to={`/datasets/${dataset.id}/versions/${item.id}`} className="mono">{item.version}</Link><span>{formatNumber(item.sizeMb)} МБ</span><span className="mono">{item.rowsLabel}</span><span>{item.author}<small>{formatDate(item.createdAt)}</small></span><Link to={`/datasets/${dataset.id}/versions/${item.id}`}>Детали</Link></div>)}</div> : <EmptyState title="Нет зарегистрированных версий" description="Создайте первую версию набора данных." />}</Card><Card className="lineage-card"><h2><AppIcon name="gitBranch" size={22} aria-hidden />Lineage</h2><h3>Источник</h3>{lineage.upstream.map((node) => <span className="lineage-node" key={node.id}>{node.name}</span>)}<h3>Используется в</h3>{lineage.downstream.map((node) => node.href ? <Link className="lineage-node" to={node.href} key={node.id}>{node.name}</Link> : <span className="lineage-node" key={node.id}>{node.name}</span>)}</Card></div></section>;
}
